#env
.env
.env.*

# Logs
/logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
.vite
secrets.env
*.env

# Ops handover notes — contain raw VPS IPs/port maps; never commit
CTO_HANDOVER*.md

# Cloudflare Worker (workers/api): local secrets + wrangler cache.
# .dev.vars.example is a different filename and stays tracked.
.dev.vars
.wrangler/

# Compiled Python (a stray literal $HOME dir once got committed this way)
*.pyc
__pycache__/

# Knowledge-graph output from the /graphify skill — a generated artifact of the
# repo's own contents, regenerable on demand and large. Never a source of truth.
graphify-out/

# Generated run reports from --report (evidence lives on the PR, not in git)
agent/*report*.json
