livekit==1.1.13
livekit-api==1.2.0
python-dotenv==1.2.1
# Move 2b — RVC bridge
websockets==15.0.1
numpy==2.0.2
scipy==1.13.1
pytest==8.4.2
# Phase 1 — capture diagnostics (capture.py / analyze_capture.py)
aiofiles==25.1.0
matplotlib==3.9.4
# Phase 3 — Silero VAD gate (vad.py). CPU inference on the agent host; this
# torch pin is the AGENT venv only — the RVC pod's cu128 pin lives in runbook.md
silero-vad==6.2.1
torch==2.8.0
torchaudio==2.8.0
